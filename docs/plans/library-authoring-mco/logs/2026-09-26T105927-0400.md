+++
type = "plan_log"
id = "library-authoring-mco-2026-09-26T105927-0400"
plan_id = "library-authoring-mco"
step_id = "file-monkey-issues"
created = "2026-09-26T10:59:27-04:00"
+++

# Log: file-monkey-issues

# altium-monkey issues filed

Filed 2026-09-26 on `wavenumber-eng/altium_monkey_dev`. Findings were grouped into five issues, each with a public-API reproduction run against altium-monkey 2026.9.22. No altium-monkey code was changed.

| Issue | Plan findings | Topic |
| --- | --- | --- |
| [#115](https://github.com/wavenumber-eng/altium_monkey_dev/issues/115) | 1, 2, 3, 7 (helper keyword) | Multi-part shared ownership (-1 vs 0) across helpers, rendering and placement; display modes not filtered on placement |
| [#116](https://github.com/wavenumber-eng/altium_monkey_dev/issues/116) | 5, 6 | No pin-scoped parameter authoring API; `hidden_net_name`, package length and propagation delay lost on save |
| [#117](https://github.com/wavenumber-eng/altium_monkey_dev/issues/117) | 8, 7 (stale record) | No typed component-kind/component-record setters; `component_record` override goes stale |
| [#118](https://github.com/wavenumber-eng/altium_monkey_dev/issues/118) | 4, 11 | Helper radii truncate to 10-mil steps; `add_rectangle` docstring units; README relative doc links |
| [#119](https://github.com/wavenumber-eng/altium_monkey_dev/issues/119) | 9, 10 | PcbLib region arc segments, authored state invisible before save, footprint parameter group layout |

Related existing issue: #109 (PinUniqueId in the schematic compiler).

## Refinements found while writing the reproductions

- Only `add_rectangle`'s docstring says "10-mil units"; the other helpers state no unit.
- The package README uses relative `docs/*.md` links. These do not resolve on PyPI and the files are not in the wheel.
- The public footprint API is broader than the plan assumed: `add_pad`, `add_custom_pad`, `add_track`, `add_arc`, `add_fill`, `add_via`, `add_region`, `add_text`, component bodies and `add_embedded_3d_model` all exist. The only missing piece is arc segments in region outlines.
- New finding (#119): `AltiumPcbLib.footprints` is empty until `save()`. This affects Cruncher's in-session duplicate-footprint check and any later lookup of a footprint created earlier in the same MCO run.

Follow-up: record the altium-monkey release that resolves each issue here, and pin that release before the dependent steps close.
